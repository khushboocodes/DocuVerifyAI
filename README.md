
  # Clone website with login profile

  This is a code bundle for Clone website with login profile. The original project is available at https://www.figma.com/design/mRupvfcVxhdXz1h4DMiqdo/Clone-website-with-login-profile.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  